// array declaration
let arr = [];

//methods in arrays
// push method
arr.push(1);
arr.push(2);
console.log(arr);
// pop method
arr.pop();
console.log(arr);
// shift method insert element at the start of array
arr.shift();
console.log(arr);
// unshift method ann element at the start of array
arr.unshift(1);